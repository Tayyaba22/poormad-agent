name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed PoorMad tool gateway. Also powers the 'PoorMad Subscription' UX flow that bills usage to a PoorMad subscription."
author: PoorMad
kind: backend
provides_browser_providers:
  - browser-use
